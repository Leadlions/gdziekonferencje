---
import Base from './Base.astro';

interface Props {
  title: string;
  description: string;
  breadcrumbs?: Array<{ name: string; url: string }>;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
}

const { title, description, breadcrumbs, jsonLd } = Astro.props;
---

<Base title={title} description={description} breadcrumbs={breadcrumbs} jsonLd={jsonLd}>
  <div class="mx-auto max-w-4xl px-4 py-10 sm:px-6">
    <slot />

    <!-- DISCLAIMER pod artykulem -->
    <div class="mt-12 rounded-xl border border-amber-100 bg-amber-50 px-5 py-4 text-xs leading-relaxed text-amber-800">
      <strong class="font-semibold">Wazna informacja:</strong>
      Powyzszy artykul ma charakter wylacznie edukacyjny i informacyjny. Nie jest pisany przez lekarza
      ani weryfikowany przez specjalistow medycznych. Tresc powstala na podstawie ogolnodostepnych
      zrodel i nie stanowi porady klinicznej ani rekomendacji terapeutycznej.
      W sprawach klinicznych zawsze konsultuj sie z odpowiednim specjalista.
    </div>
  </div>
</Base>
